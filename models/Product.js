// Tạo lớp đối tượng Product
function Product(id, img, name, comment,url) {
  // thuộc tính (properties)
  this.id = id;
  this.img = img;
  this.name = name;
  this.comment = comment;
  this.url = url;
}

// Phương thức (methods)
// Product.prototype.doSomething = function () {};
